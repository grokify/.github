name: CodeQL SAST
on:
  push:
    branches:
      - $default-branch
  pull_request:
    branches:
      - $default-branch
  schedule:
    - cron: '30 1 * * 0'
  workflow_dispatch:

jobs:
  codeql:
    uses: grokify/.github/.github/workflows/go-codeql.yaml@main
